<svg width="1200" height="260" viewBox="0 0 1200 260" xmlns="http://www.w3.org/2000/svg" role="img" aria-labelledby="title desc">
  <title id="title">Omid – Senior Fullstack Developer</title>
  <desc id="desc">Next.js, TypeScript, MongoDB, Security-focused, Clean Architecture</desc>

  <defs>
    <!-- Background gradient -->
    <linearGradient id="bgGradient" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#020617"/>
      <stop offset="40%" stop-color="#020617"/>
      <stop offset="100%" stop-color="#020617"/>
    </linearGradient>

    <!-- Accent radial highlight -->
    <radialGradient id="radialGlow" cx="20%" cy="20%" r="80%">
      <stop offset="0%" stop-color="#22c55e" stop-opacity="0.35"/>
      <stop offset="35%" stop-color="#38bdf8" stop-opacity="0.2"/>
      <stop offset="70%" stop-color="#a855f7" stop-opacity="0"/>
    </radialGradient>

    <!-- Accent line gradient -->
    <linearGradient id="accentGradient" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#22c55e"/>
      <stop offset="50%" stop-color="#38bdf8"/>
      <stop offset="100%" stop-color="#a855f7"/>
    </linearGradient>

    <!-- Soft blur shadow -->
    <filter id="softShadow" x="-20%" y="-20%" width="140%" height="140%">
      <feDropShadow dx="0" dy="12" stdDeviation="18" flood-color="#000000" flood-opacity="0.7"/>
    </filter>

    <!-- Tag pill blur -->
    <filter id="pillBlur" x="-20%" y="-20%" width="140%" height="140%">
      <feGaussianBlur stdDeviation="6"/>
    </filter>
  </defs>

  <!-- Background -->
  <rect width="1200" height="260" fill="url(#bgGradient)" rx="28" />

  <!-- Glow -->
  <rect width="1200" height="260" fill="url(#radialGlow)" rx="28" />

  <!-- Card container -->
  <g filter="url(#softShadow)">
    <rect x="40" y="36" width="1120" height="188" rx="24" fill="#020617" stroke="#111827" stroke-width="1.5" />
  </g>

  <!-- Accent line bottom -->
  <rect x="80" y="200" width="1040" height="3" fill="url(#accentGradient)" rx="1.5" />

  <!-- Left: Name / Title -->
  <text x="90" y="110" fill="#e5e7eb" font-family="system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif" font-size="42" font-weight="700">
    Omid Soleimani
  </text>

  <text x="90" y="145" fill="#9ca3af" font-family="system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif" font-size="20">
    Senior Fullstack Developer · Next.js · TypeScript · MongoDB
  </text>

  <text x="90" y="175" fill="#6b7280" font-family="system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif" font-size="16">
    Security-first · OIDC · Auth0 · Clean Architecture · Internal APIs
  </text>

  <!-- Right: Tech tags -->
  <!-- Blur background pills -->
  <g filter="url(#pillBlur)" opacity="0.55">
    <rect x="720" y="80" width="360" height="40" rx="20" fill="#020617" />
    <rect x="760" y="128" width="280" height="34" rx="17" fill="#020617" />
    <rect x="810" y="168" width="180" height="30" rx="15" fill="#020617" />
  </g>

  <!-- Tag text -->
  <text x="738" y="105" fill="#e5e7eb" font-family="system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif" font-size="17">
    🔒 OIDC · Auth0 · Protected Routes · Middleware
  </text>

  <text x="778" y="152" fill="#a5b4fc" font-family="system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif" font-size="15">
    Modular · DX-oriented · Server Components
  </text>

  <text x="828" y="180" fill="#4ade80" font-family="system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif" font-size="14">
    MongoDB · PostgreSQL
  </text>
</svg>
